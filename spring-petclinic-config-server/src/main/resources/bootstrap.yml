server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.daumkakao.com/apm/spring-petclinic-microservices-config
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
---
spring:
  profiles: krane
  cloud:
    config:
      server:
        git:
          uri: file:////kakao/service/spring-petclinic-microservices-config
