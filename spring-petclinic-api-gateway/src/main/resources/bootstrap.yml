spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: api-gateway
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
---
spring:
  profiles: krane
  cloud:
    config:
      uri: http://pet-config-server.pg1.krane.9rum.cc:8888
